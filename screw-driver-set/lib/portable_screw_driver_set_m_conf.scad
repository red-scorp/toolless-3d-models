/* Portable screw driver container size M configuration file */

include</portable_screw_driver_set_common_conf.scad>;

inner_container_len = 60;
inner_container_dia = 25;
inner_container_cone = 8;

outer_hull_len = 80;
outer_hull_dia = 35;
outer_hull_cone = 8;

outer_rib_cnt = 8;
outer_rib_dia = 10;
outer_rib_offset = 10;

hexal_hole_depth = 15;

lock_cnt = 3;

lid_len = 8;
